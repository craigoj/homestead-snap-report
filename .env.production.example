# ============================================================================
# PRODUCTION ENVIRONMENT CONFIGURATION TEMPLATE
# ============================================================================
# DO NOT commit actual production values to git!
# Copy this file and configure in your deployment platform (Netlify/Vercel)
#
# Last Updated: 2025-11-03
# ============================================================================

# ----------------------------------------------------------------------------
# SUPABASE CONFIGURATION (REQUIRED)
# ----------------------------------------------------------------------------
# Get these from: https://app.supabase.com/project/YOUR_PROJECT/settings/api
# Use PRODUCTION project credentials (not development)
NEXT_PUBLIC_SUPABASE_URL="https://your-production-project.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your_production_anon_key_here"

# Alternative naming (for compatibility with existing code)
VITE_SUPABASE_URL="https://your-production-project.supabase.co"
VITE_SUPABASE_PUBLISHABLE_KEY="your_production_anon_key_here"
VITE_SUPABASE_PROJECT_ID="your-production-project-id"

# ----------------------------------------------------------------------------
# APPLICATION CONFIGURATION (REQUIRED)
# ----------------------------------------------------------------------------
# Your production domain (e.g., https://snapassetai.com)
NEXT_PUBLIC_APP_URL="https://your-production-domain.com"
NEXT_PUBLIC_SITE_URL="https://your-production-domain.com"

# Environment identifier
NEXT_PUBLIC_APP_ENV="production"
NODE_ENV="production"

# ----------------------------------------------------------------------------
# STRIPE PAYMENT INTEGRATION (REQUIRED)
# ----------------------------------------------------------------------------
# Get from: https://dashboard.stripe.com/apikeys
# ⚠️ CRITICAL: Use LIVE keys for production (pk_live_..., sk_live_...)
# DO NOT use test keys (pk_test_..., sk_test_...) in production!

# Public key (safe to expose to client)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_live_your_live_publishable_key_here"

# Secret key (NEVER expose to client - server-side only)
STRIPE_SECRET_KEY="sk_live_your_live_secret_key_here"

# Webhook secret (from Stripe Dashboard → Developers → Webhooks)
# Create a production webhook endpoint: https://your-domain.com/api/stripe/webhook
STRIPE_WEBHOOK_SECRET="whsec_your_production_webhook_secret_here"

# Stripe Product/Price IDs (create in Stripe Dashboard → Products)
# These are different from test mode - get from production Stripe account
NEXT_PUBLIC_STRIPE_PRICE_MONTHLY="price_your_production_monthly_id"
NEXT_PUBLIC_STRIPE_PRICE_ANNUAL="price_your_production_annual_id"

# ----------------------------------------------------------------------------
# EBAY INTEGRATION (OPTIONAL - Required for automated asset valuation)
# ----------------------------------------------------------------------------
# Get from: https://developer.ebay.com/my/keys
# ⚠️ Use PRODUCTION credentials (not sandbox)

EBAY_APP_ID="your_production_app_id"
EBAY_CERT_ID="your_production_cert_id"
EBAY_DEV_ID="your_production_dev_id"

# eBay OAuth credentials (if using OAuth flow)
EBAY_CLIENT_ID="your_production_client_id"
EBAY_CLIENT_SECRET="your_production_client_secret"

# ----------------------------------------------------------------------------
# THIRD-PARTY SERVICES (OPTIONAL - Used in Supabase Edge Functions)
# ----------------------------------------------------------------------------

# OpenAI API (for OCR processing in Edge Functions)
# Get from: https://platform.openai.com/api-keys
# ⚠️ Use a production API key with appropriate rate limits
OPENAI_API_KEY="sk-your_production_openai_key"

# SendGrid (for email notifications in Edge Functions)
# Get from: https://app.sendgrid.com/settings/api_keys
# Create a production API key with Send Mail permissions
SENDGRID_API_KEY="SG.your_production_sendgrid_key"

# Google Cloud (for enhanced OCR - optional)
# Get from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLOUD_API_KEY="your_production_google_cloud_key"

# ----------------------------------------------------------------------------
# DATABASE (OPTIONAL - For advanced features)
# ----------------------------------------------------------------------------
# Direct PostgreSQL connection string for advanced queries
# Get from: Supabase → Project Settings → Database → Connection string
# Format: postgresql://postgres:[password]@db.PROJECT_ID.supabase.co:5432/postgres
POSTGRES_CONNECTION_STRING="postgresql://postgres:YOUR_DB_PASSWORD@db.your-project.supabase.co:5432/postgres"

# ----------------------------------------------------------------------------
# INTEGRATIONS (OPTIONAL)
# ----------------------------------------------------------------------------

# GitHub Personal Access Token (for GitHub MCP integration)
# Generate at: https://github.com/settings/tokens
# Scopes needed: repo, workflow, read:org
GITHUB_TOKEN="ghp_your_production_token"

# Slack Integration (optional)
# Get from: https://api.slack.com/apps
SLACK_BOT_TOKEN="xoxb-your-production-slack-bot-token"
SLACK_TEAM_ID="T01234ABCDE"

# ----------------------------------------------------------------------------
# SECURITY NOTES
# ----------------------------------------------------------------------------
#
# 1. NEVER commit this file with actual values to git
# 2. Store actual values in your deployment platform (Netlify/Vercel)
# 3. Use different keys for production vs. development/staging
# 4. Rotate keys regularly, especially after team member changes
# 5. Use environment variable scopes (production-only) when available
# 6. Enable webhook signature verification for all webhooks
# 7. Monitor API key usage in respective dashboards
# 8. Set up alerts for unusual API usage patterns
#
# For detailed setup instructions, see: PRODUCTION_ENV_SETUP.md
# ============================================================================
